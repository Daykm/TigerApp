package com.daykm.tiger.features.view;

public class BindingAdapters {
/*
    @BindingAdapter("app:remoteSrc")
    public static void setImageSrc(SimpleDraweeView view, String url) {
        view.setImageURI(url);
    }

    @BindingAdapter("app:twitterHeader")
    public static void setTwitterHeader(SimpleDraweeView view, User user) {
        if(user != null) {
            if(user.profile_banner_url != null && user.profile_banner_url.length() > 0) {
                view.setImageURI(user.profile_banner_url);
            } else {
                view.setBackgroundColor(Color.parseColor("#" + user.profile_link_color));
            }
        }
    }

    @BindingAdapter("app:statusImagePreview")
    public static void setImagePreview(SimpleDraweeView view, Status status) {
        if(status.entities != null
                && status.entities.media.size() > 0
                && Media.PHOTO.equals(status.entities.media.first().type)) {

                view.setImageURI(status.entities.media.first().media_url_https + ":thumb");
        }
    }

    @BindingAdapter("app:statusImageFull")
    public static void setImageFull(SimpleDraweeView view, Status status) {
        if(status.entities != null
                && status.entities.media.size() > 0
                && Media.PHOTO.equals(status.entities.media.first().type)) {

            view.setImageURI(status.entities.media.first().media_url_https + ":large");
        }
    }

    @BindingAdapter("app:timestamp")
    public static void setTimestamp(RelativeTextClock view, String timestamp) {
        view.setStartingTimeStamp(Long.parseLong(timestamp));
    }*/
}
